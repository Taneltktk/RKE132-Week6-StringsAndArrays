
string[] colors = { "red", "green", "blue", "pink", "yellow" };

//for (int i = 0; < i colors.Length; int++ ){
//    }


foreach (string color in colors)
{
    colors = colors.ToArray();
}

Console.WriteLine(colors);